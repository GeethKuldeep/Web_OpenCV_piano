import 'package:flutter/material.dart';

GlobalKey _keyblack1 = GlobalKey();
GlobalKey _keyblack2 = GlobalKey();
GlobalKey _keyblack3 = GlobalKey();
GlobalKey _keyblack4 = GlobalKey();
GlobalKey _keyblack5 = GlobalKey();
GlobalKey _keyblack6 = GlobalKey();
GlobalKey _keyblack7 = GlobalKey();
GlobalKey _keywhite1 = GlobalKey();
GlobalKey _keywhite2 = GlobalKey();
GlobalKey _keywhite3 = GlobalKey();
GlobalKey _keywhite4 = GlobalKey();
GlobalKey _keywhite5 = GlobalKey();


getSizes1() {
  final RenderBox renderBoxRed = _keyblack1.currentContext.findRenderObject();
  final sizeRed = renderBoxRed.size;
  print("SIZE of Red: $sizeRed");
}
getPositions1() {
      final RenderBox renderBoxRed = _keyblack1.currentContext.findRenderObject();
      final positionRed = renderBoxRed.localToGlobal(Offset.zero);
      print("POSITION of Red: $positionRed ");
}
getSizes2() {
  final RenderBox renderBoxRed = _keyblack2.currentContext.findRenderObject();
  final sizeRed = renderBoxRed.size;
  print("SIZE of Red: $sizeRed");
}
getPositions2() {
  final RenderBox renderBoxRed = _keyblack2.currentContext.findRenderObject();
  final positionRed = renderBoxRed.localToGlobal(Offset.zero);
  print("POSITION of Red: $positionRed ");
}
getSizes3() {
  final RenderBox renderBoxRed = _keyblack3.currentContext.findRenderObject();
  final sizeRed = renderBoxRed.size;
  print("SIZE of Red: $sizeRed");
}
getPositions3() {
  final RenderBox renderBoxRed = _keyblack3.currentContext.findRenderObject();
  final positionRed = renderBoxRed.localToGlobal(Offset.zero);
  print("POSITION of Red: $positionRed ");
}
getSizes4() {
  final RenderBox renderBoxRed = _keyblack4.currentContext.findRenderObject();
  final sizeRed = renderBoxRed.size;
  print("SIZE of Red: $sizeRed");
}
getPositions4() {
  final RenderBox renderBoxRed = _keyblack4.currentContext.findRenderObject();
  final positionRed = renderBoxRed.localToGlobal(Offset.zero);
  print("POSITION of Red: $positionRed ");
}
getSizes5() {
  final RenderBox renderBoxRed = _keyblack5.currentContext.findRenderObject();
  final sizeRed = renderBoxRed.size;
  print("SIZE of Red: $sizeRed");
}
getPositions5() {
  final RenderBox renderBoxRed = _keyblack5.currentContext.findRenderObject();
  final positionRed = renderBoxRed.localToGlobal(Offset.zero);
  print("POSITION of Red: $positionRed ");
}
getSizes6() {
  final RenderBox renderBoxRed = _keyblack6.currentContext.findRenderObject();
  final sizeRed = renderBoxRed.size;
  print("SIZE of Red: $sizeRed");
}
getPositions6() {
  final RenderBox renderBoxRed = _keyblack6.currentContext.findRenderObject();
  final positionRed = renderBoxRed.localToGlobal(Offset.zero);
  print("POSITION of Red: $positionRed ");
}
getSizes7() {
  final RenderBox renderBoxRed = _keyblack7.currentContext.findRenderObject();
  final sizeRed = renderBoxRed.size;
  print("SIZE of Red: $sizeRed");
}
getPositions7() {
  final RenderBox renderBoxRed = _keyblack7.currentContext.findRenderObject();
  final positionRed = renderBoxRed.localToGlobal(Offset.zero);
  print("POSITION of Red: $positionRed ");
}
getSizes8() {
  final RenderBox renderBoxRed = _keywhite1.currentContext.findRenderObject();
  final sizeRed = renderBoxRed.size;
  print("SIZE of Red: $sizeRed");
}
getPositions8() {
  final RenderBox renderBoxRed = _keywhite1.currentContext.findRenderObject();
  final positionRed = renderBoxRed.localToGlobal(Offset.zero);
  print("POSITION of Red: $positionRed ");
}
getSizes9() {
  final RenderBox renderBoxRed = _keywhite2.currentContext.findRenderObject();
  final sizeRed = renderBoxRed.size;
  print("SIZE of Red: $sizeRed");
}
getPositions9() {
  final RenderBox renderBoxRed = _keywhite2.currentContext.findRenderObject();
  final positionRed = renderBoxRed.localToGlobal(Offset.zero);
  print("POSITION of Red: $positionRed ");
}
getSizes10() {
  final RenderBox renderBoxRed = _keywhite3.currentContext.findRenderObject();
  final sizeRed = renderBoxRed.size;
  print("SIZE of Red: $sizeRed");
}
getPositions10() {
  final RenderBox renderBoxRed = _keywhite3.currentContext.findRenderObject();
  final positionRed = renderBoxRed.localToGlobal(Offset.zero);
  print("POSITION of Red: $positionRed ");
}
getSizes11() {
  final RenderBox renderBoxRed = _keywhite4.currentContext.findRenderObject();
  final sizeRed = renderBoxRed.size;
  print("SIZE of Red: $sizeRed");
}
getPositions11() {
  final RenderBox renderBoxRed = _keywhite4.currentContext.findRenderObject();
  final positionRed = renderBoxRed.localToGlobal(Offset.zero);
  print("POSITION of Red: $positionRed ");
}
getSizes12() {
  final RenderBox renderBoxRed = _keywhite5.currentContext.findRenderObject();
  final sizeRed = renderBoxRed.size;
  print("SIZE of Red: $sizeRed");
}
getPositions12() {
  final RenderBox renderBoxRed = _keywhite5.currentContext.findRenderObject();
  final positionRed = renderBoxRed.localToGlobal(Offset.zero);
  print("POSITION of Red: $positionRed ");
}


